AWS_PROFILE=
AWS_REGION=

# Bucket names are globally unique across ALL of AWS, not just your account --
# the account ID suffix is what guarantees that without a random string.
SUPPORT_AI_BUCKET_NAME=

# The naive keyword-triage Lambda created in Wednesday's console session.
# /api/v1/analysis/triage invokes it to compare against Comprehend.
SUPPORT_AI_LAMBDA_FUNCTION_NAME=