[project]
name = "ai-genai-fundamentals"
version = "0.1.0"
description = "Tokenization and embeddings, made concrete -- no API keys, no cloud calls."
requires-python = ">=3.11"
dependencies = [
    "tiktoken>=0.7",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"