# enabling tag propagation so any tags created in EB are added to the launch templates that EB uses to create EC2 instances

option_settings:
  aws:autoscaling:launchconfiguration:
    LaunchTemplateTagPropagationEnabled: true